name: CI (CentOS 7.7.1908)

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch: {}

jobs:
  pack:
    name: Pack (offline bundle)
    runs-on: ubuntu-latest
    container: centos:7.7.1908
    steps:
      - name: Prepare CentOS 7 EOL Environment and Install Dependencies
        run: |
          set -euo pipefail
          # Step 1: Point to the CentOS 7 vault repositories as it's EOL.
          cat <<'EOF' > /etc/yum.repos.d/CentOS-Base.repo
          [base]
          name=CentOS-$releasever - Base
          baseurl=http://vault.centos.org/7.9.2009/os/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          [updates]
          name=CentOS-$releasever - Updates
          baseurl=http://vault.centos.org/7.9.2009/updates/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          [extras]
          name=CentOS-$releasever - Extras
          baseurl=http://vault.centos.org/7.9.2009/extras/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          EOF
          sed -i 's/mirror.centos.org/vault.centos.org/g' /etc/yum.repos.d/*.repo
          sed -i 's/mirrorlist.centos.org/vault.centos.org/g' /etc/yum.repos.d/*.repo
          yum clean all
          yum -y makecache
          yum -y groupinstall "Development Tools"
          yum -y install git which findutils coreutils wget tar make gcc gcc-c++ unzip cpio yum-utils environment-modules xz bzip2 python3

      - name: Checkout
        # CRITICAL FIX: Pin to a specific v3.x.x version that is known to use Node.js 16.
        # This avoids issues with floating tags and runner overrides.
        uses: actions/checkout@v3.6.0

      - name: Cache packages
        # CRITICAL FIX: Pin to a specific v3.x.x version.
        uses: actions/cache@v3.3.3
        with:
          path: |
            packages
          key: centos77-pack-${{ hashFiles('software.conf') }}

      - name: Pack offline bundle
        shell: bash
        run: |
          set -euo pipefail
          source /etc/profile.d/modules.sh || true
          chmod +x ./omw
          ./omw --pack

      - name: Upload offline bundle and packages
        # CRITICAL FIX: Pin to a specific v3.x.x version.
        uses: actions/upload-artifact@v3.1.3
        with:
          name: omw-offline-bundle
          path: |
            $HOME/omw-offline-bundle-*.tar.gz
            packages/SHA256SUMS
            packages/**

  build-all:
    name: Build all software
    runs-on: ubuntu-latest
    needs: pack
    container: centos:7.7.1908
    timeout-minutes: 360
    steps:
      - name: Prepare CentOS 7 EOL Environment and Install Dependencies
        run: |
          set -euo pipefail
          cat <<'EOF' > /etc/yum.repos.d/CentOS-Base.repo
          [base]
          name=CentOS-$releasever - Base
          baseurl=http://vault.centos.org/7.9.2009/os/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          [updates]
          name=CentOS-$releasever - Updates
          baseurl=http://vault.centos.org/7.9.2009/updates/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          [extras]
          name=CentOS-$releasever - Extras
          baseurl=http://vault.centos.org/7.9.2009/extras/$basearch/
          gpgcheck=1
          gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7
          EOF
          sed -i 's/mirror.centos.org/vault.centos.org/g' /etc/yum.repos.d/*.repo
          sed -i 's/mirrorlist.centos.org/vault.centos.org/g' /etc/yum.repos.d/*.repo
          yum clean all
          yum -y makecache
          yum -y install devtoolset-11 git which findutils coreutils wget tar make gcc gcc-c++ unzip cpio rpm2cpio yum-utils environment-modules xz bzip2 python3

      - name: Checkout
        # CRITICAL FIX: Pin to a specific v3.x.x version.
        uses: actions/checkout@v3.6.0

      - name: Restore packages cache
        # CRITICAL FIX: Pin to a specific v3.x.x version.
        uses: actions/cache@v3.3.3
        with:
          path: |
            packages
          key: centos77-pack-${{ hashFiles('software.conf') }}

      - name: Build everything
        shell: bash
        run: |
          set -euo pipefail
          source /etc/profile.d/modules.sh || true
          source /opt/rh/devtoolset-11/enable || true
          chmod +x ./omw
          ./omw --all

      - name: Upload build artifacts (logs, modulefiles, bin)
        if: always()
        # CRITICAL FIX: Pin to a specific v3.x.x version.
        uses: actions/upload-artifact@v3.1.3
        with:
          name: omw-build-artifacts
          path: |
            builds/*/logs/**
            tools/modulefiles/**
            bin/**